import React from 'react';
import { ViewLayout } from '../../components';

const Settings = () => {
  return <ViewLayout title="Settings">...Settings tab panel navigation...</ViewLayout>;
};

export default Settings;
